# 从零构建「截图转题目 Word」——完整实战教程

> 本教程基于真实项目的开发全过程整理，目标是让读者能够从零复现一个**本地优先的题目数字化工具**：
> 上传题目截图 / PDF / Word → 自动识别文字与公式 → 裁剪插图并配对图注 → 生成**公式可编辑**的 Word / PDF。
>
> 教程按"最小可用版 → 逐项加功能"的顺序组织，每一步都说明**为什么这么做**、**关键代码怎么写**、**踩过什么坑**。
> 文中所有函数名、文件结构与最终项目一致，可直接对照源码阅读。

## 目录

- 第 0 章 最终效果与技术选型
- 第 1 章 环境准备（含依赖版本的坑）
- 第 2 章 最小可用版：截图 → Word 全链路
- 第 3 章 公式质量攻坚战：四层管线
- 第 4 章 Word 排版细节：字体、行距、图片尺寸
- 第 5 章 API 识别引擎与混合模式
- 第 6 章 两阶段流程：识别预览与编辑
- 第 7 章 识别后处理：选项分行 / 填空还原 / 图注配对
- 第 8 章 一页多题自动拆分
- 第 9 章 多图并行识别
- 第 10 章 插图工程化：去重、防丢图、跨题重分配
- 第 11 章 Word / PDF 文件直识别
- 第 12 章 导出 PDF（本机排版引擎转换链）
- 第 13 章 历史任务、API 用量统计与粘贴上传
- 第 14 章 界面打磨与无窗口启动
- 附录 A 踩坑速查表
- 附录 B 回归测试方法论
- 附录 C 核心数据契约

---

## 第 0 章 最终效果与技术选型

### 0.1 我们要构建什么

一个浏览器里运行的本地应用，完整流程：

1. 用户拖入题目截图（或 PDF / Word 文件，或直接 Ctrl+V 粘贴）
2. 每个文件可选识别引擎：**本地**（离线免费）/ **API**（云端大模型）/ **混合**（API 文字 + 本地裁图）
3. 后端实时推送识别进度与日志（SSE）
4. 识别结果进入**预览编辑区**：左边编辑 Markdown，右边实时渲染公式（KaTeX）与插图
5. 确认后生成 Word（公式是 Word 原生 OMML，可编辑；插图带图注；选项各自成行；1.5 倍行距仿宋正文）
6. 也可导出 PDF；历史任务保留 7 天可重复下载

### 0.2 技术选型与理由

| 选择 | 理由 | 不选什么的理由 |
|---|---|---|
| **Pix2Text** | 一站式：版面分析 + 中文 OCR + 公式初识别（pix2tex），本地运行 | 自己拼装 PaddleOCR + 版面模型 + 公式模型，工程量大 |
| **UniMERNet-small** | 公式二次识别兜底，810MB 可接受 | UniMER-Base 4.9GB 太重 |
| **Pandoc** | Markdown → docx 时自动把 LaTeX 转 **OMML**（Word 原生公式）；还能反向校验 LaTeX 合法性（texmath） | python-docx 手写 OOXML 公式太痛苦 |
| **FastAPI + SSE** | 单进程托管前端静态文件 + API，SSE 推流实现简单 | WebSocket 杀鸡用牛刀 |
| **React + AntD** | 组件齐全（Upload/Segmented/Modal），开发快 | — |
| **大模型 API（可选）** | qwen-vl-max 中文 OCR 极强，20 秒/张 | GPT-4o 无 bbox；DeepSeek 无图像接口 |

**核心设计原则（贯穿全项目）**：

> **确定性的文本处理交给正则不依赖大模型；识别失败宁可降级为图片也不输出乱码。**

## 第 1 章 环境准备（含依赖版本的坑）

### 1.1 依赖清单

```bash
# backend/requirements.txt
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9    # FastAPI 文件上传必需
pix2text>=1.1              # 首次运行自动下载模型（约 1-2GB）
unimernet==0.2.3
transformers==4.46.3       # 不要用新版！见下
tokenizers==0.20.3
Pillow>=10.0
numpy
pywin32>=306; sys_platform == "win32"   # docx 转 PDF 用 COM
```

### 1.2 版本兼容的坑（已踩平，照抄即可）

| 组件 | 锁定版本 | 原因 |
|---|---|---|
| torch | 2.5.1+cpu | 新版在本机 c10.dll 初始化失败 |
| torchvision | 0.20.1 | 配套 torch 2.5.1 |
| onnxruntime | 1.20.1 | 新版同样 DLL 失败 |
| transformers | 4.46.3 | unimernet 声明要 4.42.4，但与 pix2text 的 tokenizer 冲突；4.46.3 + eager 补丁可共存 |

安装顺序建议：

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate                                # Windows
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install -r requirements.txt
```

### 1.3 外部组件

- **Pandoc**：系统安装（`winget install pandoc`）或放便携版到 `backend/pandoc/pandoc.exe`。代码里两级查找（`find_pandoc()`：先 PATH 后便携版）。
- **UniMERNet 权重**：官方 `unimernet_small.pth` + `infer.yaml` 放 `backend/models/unimernet_small/`。缺失时跳过二次识别，不影响主流程。
- **Node.js**：仅构建前端需要（`npm install && npm run build` → `frontend/dist`），运行时不依赖 Node。
- **加速技巧**：GitHub 慢就走 PyPI；HuggingFace 用 `https://hf-mirror.com` 镜像 + curl 直接下载。

## 第 2 章 最小可用版：截图 → Word 全链路

### 2.1 架构

```
浏览器 → FastAPI（8000 端口，同时托管 frontend/dist + API）
           ├─ Pix2Text：版面分析 + 文字 OCR + 公式初识别
           ├─ 公式管线（第 3 章）
           └─ Pandoc：Markdown → docx（公式自动转 OMML）
```

单进程是关键简化：**不需要前端 dev server、不需要反向代理**，用户只需开一个程序。

### 2.2 后端骨架（api.py）

三个接口就够跑通 MVP：

```python
@app.post("/upload")            # 收图 → 存临时目录 → 返回 task_id
@app.get("/process/{task_id}")  # SSE：边识别边推日志/进度，完成后推结果
@app.get("/download/{task_id}") # 返回生成的 docx
```

SSE 的实现要点：后台线程跑识别，通过 `queue.Queue` 把事件喂给流式响应：

```python
log_queue: queue.Queue = queue.Queue()
threading.Thread(target=process_task, args=(task_id, log_queue), daemon=True).start()

def event_stream():
    while True:
        event = log_queue.get(timeout=300)
        yield f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
        if event["kind"] in ("done", "error"):
            break

return StreamingResponse(event_stream(), media_type="text/event-stream")
```

静态托管必须挂在**所有 API 路由之后**，否则 `/` 抢走路由匹配：

```python
app.mount("/", StaticFiles(directory=str(FRONTEND_DIST), html=True), name="frontend")
```

### 2.3 识别封装（recognizer.py）

Pix2Text 一次调用拿到整页结果：

```python
page = p2t.recognize_page(image_path)
for item in page.elements:
    # item.type: TEXT / FIGURE / TABLE / FORMULA / IGNORED ...
    # item.text: 识别文本；item.box: 元素框；item.meta: 逐 token 明细（含公式位置）
```

产出统一的**元素契约**（这是全项目的数据枢纽，附录 C 详述）：

```python
{"type": "text"|"formula"|"image", "text": ..., "image_path": ..., "box": [x1,y1,x2,y2]}
```

### 2.4 Markdown 组装与 docx 生成

`converter.elements_to_markdown()`：文字段落用**空行**连接（不是单换行——Pandoc 会把同段落单换行折叠成空格，第 7 章还会踩一次）；图片输出 `![图注](相对路径){width=Xcm height=Ycm}`。

`docx_builder.markdown_to_docx()`：调 Pandoc 命令行：

```python
subprocess.run([pandoc, str(md_path), "-o", str(docx_path),
                "--resource-path", resource_dir], ...)
```

- `--resource-path` 指向任务目录，Markdown 里的相对图片路径才能被找到并嵌入。
- LaTeX `$...$` 自动转 OMML，**这就是"公式可编辑"的全部秘密**。

### 2.5 前端最小界面

React + AntD 五个组件：`Uploader`（拖拽上传）→ `FileList`（缩略图列表）→ `ProgressBar` + `LogView`（SSE 驱动）→ `DownloadBtn`。SSE 用原生 `EventSource`：

```javascript
const es = new EventSource(`/process/${taskId}?filename=output.docx`)
es.onmessage = (e) => {
  const evt = JSON.parse(e.data)
  if (evt.kind === 'log') pushLog(evt.message)
  else if (evt.kind === 'progress') setProgress(evt)
  // ...
}
```

跑通这一章，你就有了一个能用的 OCR 转 Word 工具。接下来每一章都是质量与体验的攻坚战。

## 第 3 章 公式质量攻坚战：四层管线

pix2tex 直接输出的 LaTeX 有大量小瑕疵（`\cfrac`、`\operatorname{s i n}`、强制间距符等），直接塞进 Word 会变成**无法解析的红色乱码文本**。四层管线逐层兜底：

```
pix2tex 初识别
  → ① cleanup_latex() 清理
  → ② pandoc_accepts() 校验（texmath 能解析才算数）
  → ③ 失败则裁图交 UniMERNet 二次识别，再清理再校验
  → ④ 仍失败则裁剪为图片兜底（占位符"【公式见下图】"）
```

### 3.1 清理规则（cleanup_latex）

```python
text = text.replace(r"\cfrac", r"\frac").replace(r"\dfrac", r"\frac")  # Word 对连分式支持差
text = re.sub(r"\\operatorname\s*\{([^}]*)\}", _fix_operatorname, text) # {s i n} → {sin}
text = re.sub(r"\\operatorname\{([a-zA-Z]+)\}", _to_function, text)     # → \sin（函数名白名单）
text = text.replace(r"\!", "").replace(r"\,", " ")                       # 去强制间距
text = text.replace(r"\lrcorner", "、")  # 物理题顿号常被误识别为这个
```

### 3.2 用 Pandoc 反向校验 LaTeX

这是最妙的一步——**把 Pandoc 当 LaTeX 合法性裁判**：

```python
def pandoc_accepts(latex, display=False):
    wrapped = f"$${latex}$$" if display else f"${latex}$"
    result = subprocess.run([pandoc, "-f", "markdown+tex_math_dollars", "-t", "native"],
                            input=wrapped, capture_output=True, text=True, timeout=15)
    return "Math" in result.stdout   # 能解析成数学 AST 才放行
```

校验公式逐个起 pandoc 进程约几十毫秒，一张题十几个公式完全可接受。

### 3.3 UniMERNet 二次识别的两个坑

**坑一：transformers 4.46 下解码器崩**。UniMERNet 的解码器只支持 eager 注意力，猴子补丁解决：

```python
_orig = VisionEncoderDecoderConfig.from_pretrained.__func__
@classmethod
def _eager_from_pretrained(cls, *args, **kwargs):
    cfg = _orig(cls, *args, **kwargs)
    cfg._attn_implementation = "eager"
    for sub in ("encoder", "decoder"):
        if hasattr(cfg, sub):
            getattr(cfg, sub)._attn_implementation = "eager"
    return cfg
VisionEncoderDecoderConfig.from_pretrained = _eager_from_pretrained
```

**坑二：公式坐标是相对元素的**。`item.meta` 里每个公式的 `position` 是相对该 TEXT 元素框的坐标，裁图时必须加元素偏移：

```python
offset_x, offset_y = int(el_box[0]), int(el_box[1])
box = (x1 + offset_x, y1 + offset_y, x2 + offset_x, y2 + offset_y)
```

而且检测框系统性右偏，左侧要多补 `CROP_LEFT_EXTRA = 28` 像素。

### 3.4 兜底原则

任何一层失败都不让用户看到乱码：校验不过 → 二次识别 → 还不行 → **直接裁原图区域当图片**。Word 里宁可是一张公式截图，也不能是 `\mathbb{Z [}...` 乱码。

## 第 4 章 Word 排版细节：字体、行距、图片尺寸

Pandoc 默认模板是为英文论文准备的，中文试卷需要后处理 docx 包（docx 是 zip，直接改 XML）。

### 4.1 中文字体（本项目最深的坑）

只给 `w:rFonts` 写 `w:eastAsia="仿宋"` **不生效**——OOXML 规则里主题属性 `w:eastAsiaTheme` 优先级高于字面属性。必须三管齐下：

```python
def set_east_asian_font(docx_path, font_name):
    # ① styles.xml：删除所有 w:eastAsiaTheme 属性
    # ② styles.xml：写入字面 w:eastAsia="仿宋"
    # ③ word/theme/theme1.xml：<a:ea typeface> 也改为目标字体兜底
```

### 4.2 标题样式与行距

- Pandoc 的 Heading 是深蓝色 16pt 不加粗，不符合中文试卷习惯 → 后处理改为**加粗、纯黑、14pt**（`w:sz=28`，单位是半磅）。
- 1.5 倍行距：Normal 样式原本没有 spacing 定义，需补插 `<w:spacing w:line="360" w:lineRule="auto"/>`（360 = 1.5 倍，240 单倍）；已有 spacing 的样式只追加属性，保留段前段后。

### 4.3 插图尺寸

用 PIL 读裁剪图宽高比，等比缩放到**宽 ≤ 6cm、高 ≤ 5cm**，写成 Pandoc 属性 `![](path){width=6cm height=3.2cm}`。

## 第 5 章 API 识别引擎与混合模式

### 5.1 统一 OpenAI 兼容协议

`api_recognizer.py` 只用标准库 urllib 实现 Chat Completions 调用，预设 qwen-vl-max / GLM-4V / 豆包 / Gemini / 自定义五家。**不引入 openai SDK**——本地工具少一个依赖是一份稳定。

提示词要求模型输出 JSON：`{text: "正文 Markdown", figures: [{box 千分比坐标, caption}]}`，bbox 由后端用 PIL 裁剪。输出契约与本地引擎完全一致，下游 converter / docx_builder 零改动。

### 5.2 实测教训

- qwen-vl-max 文字公式识别**极好**（20 个公式全对，20 秒/张 vs 本地 60 秒+）；
- 但它的 **bbox 插图定位不可靠**（千分比和原生 `<box>` 都明显偏移）——插图裁剪不能交给它；
- **别指望大模型遵守格式指令**：曾让它"不要输出题号"，结果把出处标签 `[山西 2025...]` 也弄丢了。题号剥离这类确定性处理，后端正则做，提示词只说"保持原文"。

### 5.3 混合模式（推荐默认）

```
API 负责：文字 + 公式（它最强的部分）
本地负责：插图定位裁剪（DocLayout-YOLO 版面分析，约 1.6 秒，不做 OCR）
```

`recognizer.get_layout_parser()` 单独加载版面模型（与 Pix2Text 共用权重文件，加载很快）；`detect_figure_crops()` 只裁 figure/table 区域。注意插图裁剪要传 `left_extra=0`——公式框的 28px 左补偿对准确的版面框反而是污染。

密钥安全：API Key 只存浏览器 localStorage 和任务内存，**不落盘**。

## 第 6 章 两阶段流程：识别预览与编辑

识别不可能 100% 完美，必须让用户在生成 Word 前过目修改。

### 6.1 流程改造

```
旧：识别 → 直接生成 docx → 下载
新：识别 → SSE 推 preview 事件（每题 Markdown）→ 前端编辑 → POST /finalize → 生成下载
```

`/finalize` 接收编辑后的 Markdown 数组，过滤空题后合并生成。全程任务目录保留，下载接口不变。

### 6.2 预览富渲染（QuestionPreview.jsx）

markdown-it + KaTeX，渲染顺序有讲究：

```javascript
// 1. 先抽 $$/$ 公式为占位符 @@S2QWMATH0@@（防 markdown-it 破坏公式内容）
// 2. 剥掉 Pandoc 图片尺寸属性 {...}（浏览器不认）
// 3. markdown-it 渲染（html:false 防注入，breaks:true 单换行生效）
// 4. img src 改写为 /task-files/{taskId}/...（插图本体显示）
// 5. 占位符还原为 KaTeX HTML；失败显示红底原始 LaTeX 不阻塞
```

`/task-files/{task_id}/{path}` 接口要**防路径穿越**：resolve 后校验目标仍在任务目录内，越界 403。

### 6.3 顺手的体验修复

- 进度条停 67%：preview 是新的终点状态，进度条不认识 → preview/done 都算 100%；
- 生成后找不到下载按钮：finalize 成功**自动触发浏览器下载**，预览卡片也保留下载入口。

## 第 7 章 识别后处理：选项分行 / 填空还原 / 图注配对

### 7.1 选项分行（normalize_option_lines）

症状：预览里选项分行正常，**下载的 Word 里全挤一段**。根因：预览的 markdown-it 开了 `breaks:true`，Pandoc 却折叠单换行。

修复在 `elements_to_markdown`（预览可见）和 `merge_questions`（编辑后兜底）各做一遍（幂等）：

1. 公式包裹的选项标记解包：`$\mathrm{C . ~} 1 ~ 2 0 0$` → `C. $1 ~ 2 0 0$`（仅"字母+句点"开头才解，`$F_{1}$` 不受影响）；
2. 行内 `B.`/`C.`/`D.` 前强制分段；
3. 行首 `A. ` 转义为 `A\. `——**防 Pandoc fancy_lists 把选项解析成自动编号列表**（OCR 缺项时自动编号会和原题字母不符，空列表项还会被丢弃导致字母消失，真实踩到）。

### 7.2 填空还原（restore_fill_blanks）

填空下划线是孤横线，OCR 和多模态模型都会丢。启发式：`（填` / `(填` 前没有下划线则补 `\_\_\_\_\_\_`（转义防 Pandoc 解析成加粗）。API 提示词同步要求"下划线必须保留为 `______`"双保险。局限：不带"（填…）"提示的留空识别不了。

### 7.3 图注配对（_pair_captions）

插图元素带 `box` 坐标后，短标签（甲/乙/图1）按三条规则配对：

1. **独立小元素按位置**：标签水平中心落在图 x 范围内、紧跟图下沿 80px 内；
2. **大段文字尾部的图注行**（API/混合模式文字是无坐标 blob）：摘出尾部连续图注行，按**阅读顺序**分配——阅读顺序算法 `_reading_order`：纵向重叠过半算同一行，行内按 x 排（修复右侧乙图排在左侧甲图前的问题）；
3. **排序与推断**：有注插图按甲乙丙序列排，缺注的按序推断补齐。

配对成功输出 `![甲](path)`，Pandoc 转 docx 时成为图片下方的图注文字。

## 第 8 章 一页多题自动拆分

用户常截整页，一页两三题。思路（splitter.py）：**只裁页面左侧边条**（页宽 18%）做轻量 OCR 找题号，按题号 y 坐标把整页横切成单题子图。

题号识别要点（全是真实试卷喂出来的规则）：

- 题号只认段落**首行**（`line_number==0`）的 token——无缩进页面正文换行顶格，"柱体高为 40 cm"的 40 会被误检；
- 题号必须**连续递增**（prev+1，容忍 OCR 漏号到 prev+2）——正文数字自然滤掉；
- 容忍 OCR 把题号当公式：`{\bf7 .}` → 去 `\命令/{}$` 装饰后提取数字；
- **美术字题号被 MFD 吞掉**（大灰数字"5"连同出处标签吞成 `\mathbb{C}[...` 乱码）：首行、靠左、含 `[` 的乱码 token 记为乱码题号候选，按顺序推断为上一题号 +1；与上一题号同行的乱码跳过（那是出处标签）；
- 小数保护：`9.5` 不匹配题号；
- 切分上边距 `SPLIT_TOP_MARGIN=36`（美术字形比 OCR 框高 25px，小了会切到题号顶部）。

**回退安全**：不足 2 个题号或任何异常 → 整页按单题处理，静默不报错。拆分必须放在并行识别**之前**的串行阶段（Pix2Text 单例非线程安全）。

## 第 9 章 多图并行识别

`process_task` 用 `ThreadPoolExecutor(max_workers=3)` 重写：

- **API/混合是网络瓶颈**（20 秒/张），并发 3 路收益大；
- **本地引擎保持串行**（`local_lock`）：Pix2Text 单例非线程安全，且 CPU 并行只会互相抢占更慢；
- **顺序保证**：结果按 idx 存 dict，最后按上传顺序拼接，与完成顺序无关；
- 单张失败进 failed 列表，不中断整体；
- 版面分析单例加 `_layout_lock`（混合模式多线程并发调 ONNX 会话需保护）；
- 进度语义改为**完成计数**（current=已完成数）。

## 第 10 章 插图工程化：去重、防丢图、跨题重分配

这一章的规则全部来自真实试卷的毒打。

### 10.1 检测框去重（_dedup_figure_boxes）

版面分析对插图带常输出"整带大框 + 各单图小框"。规则：按面积升序保留，与已保留框的**交集超过两者中较小面积的 40%** 则丢弃（单向比会让大框永远判不重复，嵌套场景翻车过）。

### 10.2 大框回收防丢图（recover_dropped_figures）

真实案例：四宫格插图（甲乙丙丁）只检测出甲丙两幅小框 + 一个整带大框，去重丢大框 → **乙丁彻底丢失**。

修复两步：

1. **矩形减法**（`_rect_subtract`）：大框减去所有保留小框，得到未覆盖区域（标准左/右/上/下四条切分）；
2. **网格线切分**（`_split_by_grid_edges`）：多图排版通常是网格，保留小框的边缘就是网格线——区域偏高用水平切线、偏宽用垂直切线；切出的薄条（多为图注带）并入相邻条。

> 试过行投影（墨水密度）切分，乙图的弹簧秤斜线墨水稀疏会被拦腰切断。**确定性几何规则 > 启发式图像分析**。

### 10.3 跨题插图重分配（reassign_question_figures）

一页两题、插图混排在中间时，图注"（第 N 题图）"标明了归属。全部识别完成后，图注题号与所在题不符的插图搬到对应题；对不上题号的原地保留（安全兜底）。配套细节：

- 图注正则容忍 OCR 把数字包成小公式（`（第 $~ 3$ 题图）` → 清洗后匹配）；
- "第 N 题"后面跟"图"字的 token 不能当题号（负向断言），否则多切出只含图注的假题；
- 混合模式图注藏在大段文字尾部，尾部图注提取要**在识别后立即做**（`tag_figure_captions`），跨题重分配才用得上；
- 图注被大框包进插图里时，把已裁图片下沿收紧到图注上方（`_crop_caption_out`）。

### 10.4 其他两条

- **纯图页并入上一题**（`merge_figure_only_items`）：PDF 换页会把某题的插图单独滑到下一页，整项只有图没有文字时并入前一项；
- **表格一表两出**：API 已输出 Markdown 表格（含 `---`+`|` 行）时，混合模式跳过本地表格裁图；裁图元素带 `fig_kind` 标记，表格不参与图注配对（防表格被配上"戊"）。

## 第 11 章 Word / PDF 文件直识别

### 11.1 PDF → 页图（doc_loader.pdf_to_images）

PyMuPDF 按页渲染 180 DPI PNG 走现有图片管线。**为什么不抽文字层？** 试卷 PDF 的公式是字体排版，抽出来是乱码；渲染成图走 OCR 才能拿到 LaTeX。

页码范围解析 `parse_page_range`（"1-3,5"，容错全角分隔符），单次上限 20 页防整本上传。

### 11.2 Word 直转（doc_loader.docx_to_questions）

文字版 Word 走 OCR 是浪费——pandoc 直转**零误差、秒级、不占识别额度**：

```python
subprocess.run([pandoc, "-f", "docx", "-t", "markdown",
                "--extract-media", extract_dir,   # 图片提取到 extract_dir/media/
                "-o", str(md_path), docx_path])
```

OMML 公式自动转 LaTeX，内嵌图片自动提取。然后按题号行切成一道道题。三个坑：

1. **题号正则**要认 `4[2026江苏…]` 这种"题号紧跟年份来源标注"的无点号形式（pandoc 转义为 `4\[2026`）；
2. pandoc 按 `--extract-media` 入参**原样**写图片引用——传绝对路径，Markdown 里就是带反斜杠的绝对路径，必须后处理改写成相对任务目录的路径；
3. 多个 Word 同任务时媒体按 `direct/fNN/` 分目录，防 rId 重名覆盖。

### 11.3 编排：工作清单（plan）

上传接口把文件统一展开成有序 plan：

```
图片 → {"kind":"image"} ；PDF → 每页一条 image；Word 直转 → 每题一条 preset（Markdown 直接进预览，不识别）；Word OCR → docx→PDF→页图
```

`process_task` 消费 plan：preset 项短路返回（套与识别结果相同的后处理保持风格一致），image 项走正常识别。展开耗时操作（docx→PDF 走 COM 可能几十秒）放 `run_in_threadpool` 避免阻塞事件循环。

## 第 12 章 导出 PDF（本机排版引擎转换链）

公式是 OMML，只有真排版引擎能保真渲染，所以 docx → PDF 走本机软件（pdf_builder.py），优先级链：

```
Microsoft Word（COM）→ WPS（COM：wps.Application / KWPS.Application）→ LibreOffice（headless --convert-to）
```

COM 要点：

- `DispatchEx` 新建独立实例——**绝不 Quit 用户正在用的 Word/WPS 窗口**；
- 后台线程调用前必须 `pythoncom.CoInitialize()`；
- `DisplayAlerts=0` 屏蔽弹窗；
- `ExportAsFixedFormat(path, 17)` 比 SaveAs2 通用（WPS 也支持）；
- LibreOffice 需 `-env:UserInstallation=file:///临时目录`，避免与用户运行的实例抢配置锁。

接口层面：docx 总是生成（PDF 由它转换）；`both` 模式 PDF 失败降级为只给 Word 并返回 `pdf_error`；双格式自动下载错开 600ms 防浏览器拦截。

## 第 13 章 历史任务、API 用量统计与粘贴上传

### 13.1 历史任务

每个任务的关键信息（原始文件名、引擎、最终 Markdown、用量、输出文件）落盘 `temp/<task_id>/task.json`；启动时扫描恢复，**重启后历史任务仍可下载**。保留策略：无结果 2 小时清、有结果 7 天。

「重新导出」= 取回最终 Markdown + 当前输出设置调 `/finalize` 重新生成，**不重新识别**——这是最快的导出路径。密钥从不落盘，历史任务自然不含 API 配置。

### 13.2 用量统计

OpenAI 兼容响应的 `usage` 字段逐次累计到 `api_usage.json`（total + by_model，加锁写入）。不内置价格表，前端提示按服务商刊例价自行估算。

### 13.3 粘贴上传

全局 `paste` 监听，剪贴板图片直接进上传列表（自动命名 `粘贴截图_MMDD_HHMMSS.png`）；输入框聚焦时不拦截，处理中不响应。

## 第 14 章 界面打磨与无窗口启动

### 14.1 界面

- **玻璃拟态卡片**：半透明白 + backdrop blur + 白色描边 + 大柔阴影 + hover 微放大（大卡片别用大 scale，会和邻卡重叠）；
- **玻璃按钮**：锥形渐变旋转描边 + 悬停扫光 + 按下 3D 倾斜（`GlassButton.jsx`，uiverse 风格纯 CSS 移植，不新增依赖）；
- **下划线动画输入框**（`.underline-field`，聚焦蓝色下划线从左展开）；
- **WebThreads 背景**：react-bits 的 WebGL 丝线组件（ogl），`pnpm dlx shadcn@latest add @react-bits/WebThreads-JS-CSS` 安装后全屏铺底；
- 标签定宽 90px + nowrap（72px 会导致"输出文件："折行）。

### 14.2 无窗口启动

- `启动.bat`：可见阶段只做环境检查与端口探测，随后通过 `启动.vbs`（`wscript` + `sh.Run(...,0,False)`）以完全隐藏方式启动 `pythonw.exe api.py`，自动打开浏览器；
- `停止服务.bat` 调 `stop_server.ps1`，按命令行匹配 `api.py` 精准结束后台进程；
- **bat 必须 GBK 编码**（cmd 按 GBK 解析，UTF-8 中文行会乱）；`if ()` 块内 set 的变量同块内 `%var%` 读不到（延迟扩展坑）；
- 日志双保险：`backend.log`（FileHandler）+ `console.log`（stdout/stderr 重定向）——后台进程静默死亡也能排查；
- 排查"改了代码不生效"先 `netstat -ano | findstr :8000`——旧进程残留占用端口，健康检查命中的是旧进程。

---

## 附录 A 踩坑速查表

| 症状 | 根因 | 解法 |
|---|---|---|
| Word 里公式是乱码文本 | pix2tex LaTeX 小瑕疵 | 四层管线 + pandoc 校验 |
| UniMERNet 加载崩 | transformers 4.46 注意力实现 | eager 猴子补丁 |
| 公式裁图偏右漏左边 | meta 坐标相对元素 + 检测框右偏 | 加元素偏移 + 左补 28px |
| 中文字体设置了不生效 | eastAsiaTheme 优先级高于字面属性 | 删主题属性 + 写字面属性 + 改 theme1.xml |
| 选项挤一段 | markdown-it breaks 与 pandoc 折叠单换行的差异 | normalize_option_lines 补空行 |
| 选项字母变成自动编号 | pandoc fancy_lists | 行首 `A\.` 转义 |
| 大模型把出处标签弄丢 | 指令遵循不可靠 | 确定性处理全走正则不靠提示词 |
| API 插图裁到文字 | qwen bbox 不可靠 | 混合模式：本地版面分析裁图 |
| 四宫格丢图 | 去重丢了大框，乙丁只被大框覆盖 | 矩形减法回收 + 网格线切分 |
| 表格出现两次 | API 已输出 md 表格 + 本地又裁图 | skip_tables + fig_kind 标记 |
| PDF 换页多出纯图"题" | 插图滑到下一页 | 纯图项并入上一题 |
| bat 中文乱码 | cmd 按 GBK 解析 | bat 文件 GBK 编码保存 |
| 改代码不生效 | 旧进程占用 8000 端口 | netstat 查端口先杀旧进程 |
| bash 测接口公式变 formfeed | 多层转义 `\f` | 请求体写文件再发 |

## 附录 B 回归测试方法论

- 每条后处理规则都配单元测试（`backend/_test_cleanup.py`，35 项）：选项拆分、公式安全、幂等、填空、图注、回收切分、纯图合并、表格排除；
- 拆题规则配 `_test_split2.py`；历史任务配 `_test_history.py`（端到端 HTTP 冒烟）；
- 测试素材用**真实踩坑截图的检测框坐标**，不是拍脑袋编的——规则是为了解决具体问题存在的；
- SSE 接口注意：每次 GET `/process` 都会重新触发识别，测试时用 `curl -N` 一次性收事件流到文件再解析；preview 事件后流不主动关闭（前端会 close），curl 测试超时退出是正常的；
- 临时调试脚本用完即删，回归脚本保留。

## 附录 C 核心数据契约

**元素（element）**——识别管线的统一产出，converter 的唯一输入：

```python
{
  "type": "text" | "formula" | "image",
  "text": str,                # 文字 / 公式 LaTeX；image 为 ""
  "image_path": str | None,   # image 的裁剪图路径
  "box": [x1, y1, x2, y2],    # 原图坐标；API blob 文字为 None
  "caption": str | None,      # 配对后的图注（甲 / 第2题图）
  "fig_kind": "figure" | "table" | None,   # 表格不参与图注配对
}
```

**工作项（plan item）**——上传展开的统一清单：

```python
{"kind": "image",  "path": ..., "engine": "local|api|hybrid", "name": ...}
{"kind": "preset", "label": ..., "md": ...}   # Word 直转题目，跳过识别
```

**SSE 事件**：`log` / `progress` / `preview`（识别完成推题目 Markdown）/ `done` / `error`。

---

> 教程完。配套文件：`README.md`（项目介绍）、`工作总结.md`（逐次开发记录）、`API选型分析.md`（大模型选型过程）。
