# 项目工作总结（会话交接文档）

> 生成时间：2026-08-10 ｜ 供新会话快速接手使用

## 一、项目概况

**名称**：截图转题目 Word（Screenshot2QuestionWord）
**位置**：`E:\PICTURE-TO-WORD\Screenshot2QuestionWord\`
**目标**：浏览器上传题目截图 → 本地识别文字/公式/插图 → 生成公式可编辑的 Word。
**硬约束**：完全本地运行，禁止调用任何远程多模态 AI API。

## 二、最终架构（单进程版）

```
浏览器 → FastAPI（8000 端口，同时托管前端静态文件 + API）
           ├─ Pix2Text：版面分析 + 文字 OCR + 公式初识别（pix2tex）
           ├─ 公式四层管线（见下）
           └─ Pandoc（便携版）：Markdown → docx（公式转 OMML）
```

- 前端已构建为 `frontend/dist`，由 FastAPI 直接托管，**不需要 Node/vite 运行时、不需要代理**
- 启动方式：双击根目录 `启动.bat`（GBK 编码），浏览器访问 **http://localhost:8000**
- 开发调试仍可 `npm run dev`（5173，vite 已配代理到 8000）

### 项目结构

```
Screenshot2QuestionWord/
├── 启动.bat                  # 一键启动（单进程版，GBK 编码！勿用 UTF-8 编辑器保存）
├── README.md
├── sample_question.png       # 数学示例题
├── 物理题_示例输出.docx       # 旧管线输出示例
├── 物理题_双引擎版.docx       # 新管线输出示例（推荐查看）
├── backend/
│   ├── api.py                # FastAPI：/upload /process(SSE) /download /health + 静态托管 + backend.log
│   ├── recognizer.py         # 核心：Pix2Text + 公式四层管线（本次重点改造）
│   ├── converter.py          # 题目 Markdown 生成与合并
│   ├── docx_builder.py       # Pandoc 调用（系统 PATH → backend/pandoc 便携版兜底）
│   ├── requirements.txt
│   ├── pandoc/pandoc.exe     # Pandoc 3.9 便携版
│   ├── models/unimernet_small/   # UniMERNet-small 官方权重（810MB）+ infer.yaml
│   └── .venv/                # Python 3.12 虚拟环境
└── frontend/                 # React 18 + Vite + AntD 5（浅色主题，全中文）
    └── dist/                 # 已构建，后端托管
```

## 三、公式识别四层管线（recognizer.py 核心逻辑）

每个公式（含 TEXT 元素 meta 里的行内公式）依次经过：

1. **pix2tex 初识别** → `cleanup_latex()` 清理：`\cfrac/\dfrac→\frac`、`\operatorname{s i n}→\sin`（含函数名白名单）、去 `\!` `\,` `\;`、`\lrcorner→、`、`\stackrel→\frac`
2. **Pandoc(texmath) 校验** `pandoc_accepts()`：能解析 → 保留为**可编辑 Word 公式**
3. 失败 → 裁剪公式区域交 **UniMERNet-small** 二次识别 → 再清理再校验
4. 仍失败 → 裁剪为图片兜底（占位符"【公式见下图】"），保证 Word 中无乱码 LaTeX

关键配置在 `recognizer.py` 顶部：`USE_UNIMERNET_FALLBACK`、`FORMULA_IMAGE_FALLBACK`、`CROP_LEFT_EXTRA` 等。

## 四、环境依赖（已踩平的坑，勿重复踩）

| 组件 | 版本 | 备注 |
|---|---|---|
| torch | **2.5.1+cpu** | 2.13 在本机 c10.dll 初始化失败（i5-12500H 无问题，是新版兼容性） |
| torchvision | 0.20.1 | 配套 torch 2.5.1 |
| onnxruntime | **1.20.1** | 1.28 同样 DLL 初始化失败 |
| transformers / tokenizers | **4.46.3 / 0.20.3** | unimernet 声明要 4.42.4，但与 pix2text 的 tokenizer 冲突；4.46.3 + eager 补丁可共存 |
| unimernet | 0.2.3 | 用**官方 .pth 权重**；社区转换版（byb3269785）键名不兼容，已弃用删除 |
| Pandoc | 3.9 便携版 | 从 pypandoc_binary 的 wheel 里提取（GitHub 直连太慢） |

其他要点：
- UniMERNet 需 **eager 注意力补丁**（recognizer.py `get_unimernet()` 内猴子补丁 VisionEncoderDecoderConfig）
- meta 中公式 position 是**相对元素坐标**，裁剪时要加元素 box 偏移；检测框右偏，左侧补偿 `CROP_LEFT_EXTRA=28`
- `启动.bat` 必须 **GBK 编码**（cmd 按 GBK 解析，UTF-8 会导致中文行解析错乱）
- bat 陷阱：`if ()` 块内 set 的变量同块内 %var% 读不到（延迟扩展）；npm 回退路径需把 NODE_DIR 加入 PATH（vite 内部要调 node）
- 下载加速：GitHub 慢 → PyPI 快（4MB/s）；HF 用 `https://hf-mirror.com`（curl 直接下，huggingface_hub 连接不稳）

## 五、验证结论

- 数学示例题（sample_question.png）：题干 + 3 行内公式 + 几何图，全对
- 物理真题（带电小球多选）：**全部公式识别为可编辑公式，零截图兜底**（B/C/D 分式与三角函数均正确）
- 全链路（上传→SSE 进度→下载 docx→OMML 公式嵌入）多次端到端通过

## 六、2026-08-10 更新：图片尺寸限制 + 中文字体选择

1. **图片尺寸**：`converter.py` 用 PIL 读取裁剪图宽高比，生成 `![](path){width=Xcm height=Ycm}` 属性，**等比缩放，宽 ≤ 6cm、高 ≤ 5cm**（常量在 `converter.py` 顶部 `MAX_IMG_WIDTH_CM/MAX_IMG_HEIGHT_CM`）。
2. **中文字体**：前端新增"中文字体"下拉框（仿宋 / 黑体 / 楷体 / 宋体 / 微软雅黑 / 等线，**默认仿宋**）；`/process` 新增 `font` 参数（白名单校验）；`docx_builder.py` 的 `set_east_asian_font()` 在 Pandoc 生成后**后处理 styles.xml**，给所有 `<w:rFonts>` 补/改 `w:eastAsia` 属性（显式属性优先于主题字体，公式 OMML 字体不受影响）。
3. **验证**：示例题端到端通过，docx 内图片 6.00×3.17cm，25 个 rFonts 全部写入所选字体（仿宋/黑体均验证）。
4. **注意**：测试发现旧后端进程残留占用 8000 端口会导致改了代码却不生效（健康检查命中旧进程），排查时先 `netstat -ano | findstr :8000` 确认。
5. **字体修复（重要坑）**：Pandoc 模板的 `w:rFonts` 带 `w:eastAsiaTheme` 属性，按 OOXML 规则**主题属性优先级高于字面 `w:eastAsia`**，只写 `w:eastAsia` 在 Word 中不生效（显示主题字体等线）。`set_east_asian_font()` 必须同时：① 删除所有 `w:eastAsiaTheme`；② 写入字面 `w:eastAsia`；③ 把 `word/theme/theme1.xml` 的 `<a:ea typeface>` 也改为目标字体兜底。正文 run 里的 `<w:rFonts w:hint="eastAsia"/>` 只是提示，不含字体名，无需处理。

## 七、2026-08-11 更新：无窗口后台启动

- `启动.bat` 重写为**无窗口版**：可见阶段只做环境检查与端口探测，随后通过 `启动.vbs`（`wscript` + `sh.Run(...,0,False)`）以完全隐藏方式启动 `pythonw.exe api.py`，不再出现黑色终端窗口；启动后自动打开浏览器。双击时仅有一闪而过的自检窗口。
- 新增 `停止服务.bat`（调用 `stop_server.ps1`，按命令行匹配 `api.py` 精准结束后台 `pythonw` 进程）。
- 日志不受影响：`backend/backend.log`（FileHandler）+ 新增 `backend/console.log`（stdout/stderr 重定向）。
- 注意：bat 文件仍需 **GBK 编码**；停止脚本中 PowerShell 用完整路径 `%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe`，避免非常规 PATH 下找不到。

## 八、2026-08-11 更新：API 识别引擎（本地 / API 逐题可选）

- 新增 `backend/api_recognizer.py`：远程多模态 API 识别引擎。统一走 **OpenAI 兼容 Chat Completions 协议**（仅用标准库 urllib，无新增依赖），提示词要求模型输出 JSON（正文 Markdown + 插图 bbox 千分比坐标），bbox 由后端用 PIL 本地裁剪；公式段复用 `cleanup_latex()` + `pandoc_accepts()` 清理校验。输出契约与 `recognize_question` 完全一致，converter / docx_builder 零改动。
- 服务商预设（`PROVIDERS`）：qwen-vl-max（DashScope，首选）/ GLM-4V / 豆包 vision / Gemini / 自定义，全部为 OpenAI 兼容端点；选型分析见根目录 `API选型分析.md`（结论：Qwen-VL 中文 OCR + bbox grounding 最强；GPT-4o 无 bbox 不推荐；DeepSeek 无图像接口不可用）。
- `api.py`：`POST /upload` 新增 `engines`（JSON 数组，与 files 一一对应）与 `api_config`（JSON）表单字段；新增 `GET /api-providers`；处理循环按每题引擎分流本地 / API。API Key 只在任务内存中保存，不落盘。
- 前端：新增 `components/ApiSettings.jsx`（服务商/密钥/地址/模型，存 localStorage）；`FileList.jsx` 每张缩略图下新增 Segmented「本地/API」切换；`App.jsx` 上传时附带 engines 与 api_config，选择 API 但配置不全时前端拦截提示。dist 已重新构建。
- 边界：API 模式无逐公式精确坐标，公式校验失败只能保留原文并记警告（不能像本地引擎那样裁剪单个公式图片兜底）；插图裁剪依赖模型的 bbox 能力，Qwen-VL / Gemini 较稳定。

## 九、2026-08-11 更新（二）：混合模式（hybrid 引擎）+ qwen-vl-max 实测

- **实测结论**（物理题截图，含分式根号公式 + 几何插图）：
  - qwen-vl-max 文字与公式识别完美：20 个公式全部转为可编辑 OMML（含 √5kQ/5L² 类分式），单次约 20 秒（本地 CPU 引擎 1 分多钟）。
  - 但 **bbox 插图定位不可靠**（千分比 JSON 与原生 `<box>` 格式都明显偏移，裁到文字区域）；`qwen2.5-vl-72b-instruct` / `qwen-vl-max-latest` 返回 403（业务空间密钥只授权了 qwen-vl-max）。DashScope 业务空间密钥直连即可用，无需传 workspace ID；前端仍提供「业务空间 ID」可选字段（仅 qwen 显示），填了会带 `X-DashScope-WorkSpace` 请求头，实测带与不带都能正常调用 qwen-vl-max。
- **混合模式**（引擎值 `hybrid`）：API 只负责文字+公式，插图由本地版面分析裁剪。
  - `recognizer.py` 新增 `get_layout_parser()`（DocLayout-YOLO 单例，模型与 Pix2Text 共用，不跑 OCR，检测仅约 1.6 秒）与 `detect_figure_crops()`（只裁 figure/table 区域）。
  - `_crop_and_save()` 新增 `left_extra` 参数：版面分析的检测框是准的，插图裁剪传 0（不再套用公式框的左侧 28px 补偿，否则会把左侧文字带进插图）。
  - `api_recognize_question()` 新增 `skip_figures` 参数；`api.py` 处理循环按 local / api / hybrid 三分流。
  - 前端 Segmented 变为三选项（本地 / API / 混合），带 hover 说明；混合模式同样需要 API 配置校验。
- **端到端验证通过**：hybrid_test.docx 含 20 个 OMML 公式 + 1 张干净的插图（完整圆+坐标系，无文字混入）。
- 使用建议：有 API Key 时**默认选混合**；纯 API 仅用于无插图的题目。

## 十、2026-08-11 更新（三）：自动剥离题目序号

- 需求：去掉截图开头的题目序号（9. / 9、 / (9) / 第9题 等），正文顶格与"题目 N"标题左对齐。
- 实现：`converter.py` 新增 `strip_question_number()`，在 `elements_to_markdown()` 中对第一道文字元素的开头剥离一次，三种引擎（本地/API/混合）统一生效。正则含小数保护：`9.5 N` 开头的题干不会被误删。
- **教训（重要）**：曾尝试在 API 提示词里让模型"不要输出序号"，结果 qwen-vl-max 把出处标签 `[山西 2025...]` 也一起丢了（加了"必须保留"也不听）。结论：**确定性的文本处理交给后端正则，不要依赖大模型的指令遵循**；提示词只要求"保持原文，序号出处原样保留，由后端统一处理"。
- 验证：混合模式端到端通过，输出为"题目 1 / [山西 2025 高一下期末联考]如图所示……"。

## 十一、2026-08-11 更新（四）：标题样式定制

- `merge_questions()` 新增 `title_prefix` 参数；`/process` 新增 `title` 查询参数（去空白、限长 12 字符）；前端输出设置区新增"标题前缀"输入框（默认"题目"）。
- `docx_builder.py` 新增 `style_question_headings()`：Pandoc 默认模板的 Heading 是深蓝色（themeColor accent1）16pt 不加粗，不符合中文试卷习惯，后处理 styles.xml 把 Heading 1/2 改为**加粗、纯黑、14pt**（w:sz=28）。
- 验证：`title=例题` 端到端通过，首段"例题 1"，样式 加粗 + w:color=000000 + 14pt。

## 十二、2026-08-11 更新（五）：正文 1.5 倍行距

- `docx_builder.py` 新增 `set_line_spacing()`：Pandoc 模板的 Normal 样式没有 spacing 定义，需补插 `<w:pPr><w:spacing w:line="360" w:lineRule="auto"/></w:pPr>`；已有 spacing 的样式（如 BodyText 段前段后 180）只追加 `w:line/w:lineRule` 属性，段前段后保持不变。
- 360 = 1.5 倍（240 为单倍，480 为双倍），当前写死 1.5 倍；如需可选，再加前端参数即可。
- 验证：端到端通过，Normal/BodyText 均带 1.5 倍行距，docx 结构合法（python-docx 可正常打开）。

## 十三、2026-08-13/14 更新：默认混合模式 + 界面改版

- 前端新添加的截图默认引擎从 `local` 改为 `hybrid`（Uploader.jsx / FileList.jsx / App.jsx 三处兜底值同步修改）。
- 后端在 engines 参数缺失时仍兜底 `local`（离线场景更安全，不会因缺 API 配置报错）。
- **布局**：第一行「上传」整行卡片（高度随图片数量自适应）；第二行左「输出设置」右「处理进度」，两卡等高；「API 设置」收纳为输出设置卡片里的按钮，点击弹出 Modal（未填密钥点开始时自动弹出）。
- **输出设置**：输出文件 / 中文字体 / 标题前缀三项各占一行，标签 90px 定宽 + nowrap（72px 会导致"输出文件："折行的坑）。
- **输入框**：全部改为下划线动画样式（`.underline-field`，纯 CSS 移植，hover 淡蓝底、聚焦蓝色下划线从左展开；antd Input/Password/带 addon 结构均已适配，原参考代码是深色主题白字，已改为深色文字）。
- **卡片**：毛玻璃拟态（`.main-card`：半透明白 + backdrop blur 6px + 白色描边 + 大柔阴影 + hover 放大 1.02/按下回弹；页面背景改为浅色渐变以衬托模糊效果；大卡片不宜用原式的 scale(1.05)，会和邻卡重叠）。
- **按钮**：新增 `components/GlassButton.jsx`（结构 .glass-btn-wrap > button > span + .button-shadow），锥形渐变旋转描边、悬停扫光、按下 3D 倾斜；已用于 开始处理/清空文件/API 设置/下载 Word；FileList 的小型删除图标按钮保留 antd 原样。
- 样式参考原件留存于 `../button_style_ref.txt`（uiverse 风格，styled-components 语法，本项目一律纯 CSS 移植，不新增依赖）。

## 十四、2026-08-14 更新：识别预览与编辑（两阶段流程）

- 流程改为：识别 → 前端预览编辑 → 确认生成 Word。
- 后端：`/process`（SSE）识别完成后不再直接生成 docx，改为推送 `{"kind":"preview","questions":[...],"failed":[...]}`；新增 `POST /finalize/{task_id}`（请求体 questions/filename/font/title），合并编辑后 Markdown 并调 Pandoc。空内容题目会被过滤，全空报 400。
- 前端：新增预览卡片「4. 识别预览与编辑」（识别完成后出现），每题一个可编辑 TextArea，「生成 Word 文件」按钮调 /finalize，成功后出现下载按钮。
- 注意：TextArea 中 `![](figures/...){...}` 行是插图引用，提示用户保留。
- 验证：端到端通过（编辑标记文字成功写入 docx，公式/插图不受影响）。

## 十五、2026-08-14 更新（二）：预览富渲染（公式 + 插图本体）

- 预览卡片改为**左编辑 / 右预览并排**（`.preview-pair` grid，窄屏上下排列），编辑内容实时渲染。
- 新增 `GET /task-files/{task_id}/{file_path:path}`：返回任务目录内文件（预览插图用）；resolve 后校验路径必须仍在任务目录内，越界 403、不存在 404（均已实测）。
- 新增 `components/QuestionPreview.jsx`：markdown-it（html:false 防注入、breaks:true）+ KaTeX。渲染顺序：先抽 $$/$ 公式为占位符 → 剥 Pandoc 图片尺寸属性 → md 渲染 → img src 改写 `/task-files/{taskId}/...` → 占位符还原 KaTeX HTML。KaTeX 失败显示红底原始 LaTeX（class="math-error"）不阻塞。
- 新依赖：markdown-it、katex（注意 frontend 是 "type":"module"，node 测试脚本要用 .cjs 后缀）。
- 注意：KaTeX 渲染失败 ≠ Word OMML 一定失败，仅作提醒。

## 十六、2026-08-14 更新（三）：收尾修复与细节

- **进度条 67% bug**：进度事件在"开始处理第 N 张"时发出，算法 (N-1)/总数；两阶段流程后终点状态变为 preview，进度条不认识就停在 67%。修复：preview/done 均视为完成显示 100%，提示文字按阶段区分（ProgressBar.jsx）。
- **生成后找不到文件**：下载按钮原来只在"处理进度"卡片，用户在预览卡片操作看不到。修复：finalize 成功后自动触发浏览器下载 + 预览卡片加「重新下载 Word」按钮。
- **预览卡片玻璃特效**：`.preview-glass-card`（20px 强模糊 + 悬停暖色光晕 + 右上竖线装饰；橙色圆斑应用户要求已删除；覆盖了通用卡片的缩放动效避免输入抖动）。

## 十七、2026-08-14 更新（四）：题干浮动插图 + 选项插图精准识别 —— ⚠️ 已被用户主动回退，代码不在仓库中

> **状态说明（2026-08-14 晚）**：本节所述改动在三个后端文件（recognizer.py / api_recognizer.py / converter.py）中实施并验证通过，但用户测试后主动回退了这三个文件，表示不再需要该功能。**当前代码不含本节任何内容**，仅保留此技术记录供日后参考，切勿按本节假设代码现状。

针对"题干有示意图（如图所示）、选项也是图片"的题型（测试图：`../必刷题第四页第六题.png`，点电荷 + 4 个 v-t 选项图）：

- **问题 1**：版面分析把被文字环绕的题干小示意图误判为 `IGNORED` 直接丢弃 → 新增 `_is_embedded_figure_box()` 几何判定（面积占整图 0.5%~35%、非贴边长条），把这类浮动插图捡回来；页眉页脚同为 IGNORED，靠"贴顶/底 + 横跨 60% 页宽"特征排除。
  - 注意：Pix2Text 的 `recognize_page` 不返回 IGNORED 元素，本地引擎需补跑一次轻量版面分析（`get_layout_parser`，约 2 秒）捡回，与已裁插图交叠比 > 0.5 的跳过防重复。
- **问题 2**：4 个选项图 + ABCD 字母被检测为**一整块 FIGURE**，无法对应到选项 → 新增 `split_option_row()` 投影拆分（纯几何，不依赖大模型）：墨迹行带切分（下带明显矮→字母带）→ 字母带与图形带各自按列拆段 → 段数一致且 x 中心对齐才拆（2-6 个），任一条件不满足回退整图。字母带不入图，字母按 x 顺序重新生成为文字（A/B/C/D）。
- **元素契约**：image 元素新增 `bbox`（原图坐标）与 `label`（选项字母）字段；三种引擎（本地 / API / 混合）统一由 `crop_figure_elements()` 产出。
- **converter 精准落位**：带 label 的选项图配到正文中的选项锚点（`A.` / `A、` / `(A)` / 纯字母行 / 带内容选项行均可，插图紧随该选项，尺寸上限 4.5×3.5cm）；正文无锚点时按字母顺序追加在末尾；题干图排在正文下方（6×5cm 上限，不变）。几何兜底：无 label 插图 + 正文末尾纯字母行 + 原图横向一排 → 按 x 顺序配对（`_associate_letter_line()`）。
- **附带修复**：本地模式 `recognize_question` 的 FIGURE 裁剪此前误用公式的左侧 28px 补偿（会把左侧文字带进插图），已随 `crop_figure_elements` 统一为 `left_extra=0`。
- **验证**：三种引擎的插图路径均端到端通过；示例输出 `../必刷题第四页第六题_本地识别示例.docx`（5 图全部正确嵌入）。本地引擎对该图的文字 OCR 质量一般（出处行被排到正文后、`l`→`\l` 等），属既有边界，推荐混合模式（qwen 文字 + 本地插图）。
- **教训沿用**：选项字母配对坚持"几何 + 正则"的确定性路线，未让大模型输出占位符（遵循更新（三）的教训）。

## 十八、2026-08-14 更新（五）：PDF 导出

- 新增 `backend/pdf_builder.py`：docx → PDF 转换链，按优先级依次尝试 **Microsoft Word（COM）→ WPS（COM，`wps.Application` / `KWPS.Application`）→ LibreOffice（headless `--convert-to`）**，全部失败时抛出带各转换器失败原因的中文错误。公式是 OMML，只有真排版引擎能保真渲染，因此不引入 LaTeX/HTML 重排版。
- **COM 要点**：用 `DispatchEx` 新建独立实例（绝不 Quit 用户正在用的 Word/WPS 窗口）；后台线程调用前必须 `pythoncom.CoInitialize()`；`DisplayAlerts=0` 屏蔽激活弹窗；`ExportAsFixedFormat(path, 17)` 比 SaveAs2 通用（WPS 也支持）。LibreOffice 需 `-env:UserInstallation=file:///临时目录` 避免与用户运行的实例抢配置锁。
- **接口**：`/finalize` 新增 `format` 字段（`docx` 默认 / `pdf` / `both`）；docx 总是生成（PDF 由它转换，也便于补下载）；`both` 时 PDF 失败降级为只给 Word 并返回 `pdf_error`，`pdf` 单独失败则整体 500。`/download/{task_id}` 新增 `kind=docx|pdf` 查询参数。响应带 `files: {docx, pdf}` 供前端渲染下载按钮。
- **前端**：输出设置新增「导出格式」下拉（Word / PDF / Word + PDF）；文件名后缀 addonAfter 随格式联动（`.docx` / `.pdf` / `.docx+.pdf`），onBlur 同时剥两种后缀；finalize 成功后自动下载（双格式错开 600ms 防浏览器拦截）；DownloadBtn 与预览卡片按 `files` 渲染对应下载按钮。
- 依赖：后端新增 `pywin32`（requirements.txt 已加，仅 Windows）。
- 验证：HTTP 全链路冒烟通过（upload → finalize both/pdf → 双 kind 下载），PDF 内 OMML 公式（分式 kQq/r²）渲染正确。测试时踩的坑：bash 多层转义会把请求体里的 `\frac` 变成 `\f`（formfeed），与产品无关，写文件时注意。
- **注意（重要）**：本次开发前发现 recognizer.py / api_recognizer.py / converter.py 被回退到了 8-14 凌晨的旧版本；后经用户确认是**主动回退**（不再需要第十七节的插图识别功能），请勿恢复。PDF 功能独立于那些改动，不受影响。

## 十九、2026-08-15 更新：下载文件中选项挤在同一行的修复

- **问题**：预览界面选项分行正常，但下载的 Word 里一道题的 A/B/C/D 选项挤在同一个段落。
- **根因**：识别结果的 Markdown 里选项之间只有**单个换行**；前端预览用的 markdown-it 开了 `breaks:true`（单换行即换行），而 Pandoc 会把同段落内的单换行折叠成空格。
- **修复**（`converter.py` 新增 `normalize_option_lines()`，在 `merge_questions` 合并时统一执行——位于预览编辑之后，用户怎么改都生效）：
  1. 行首选项标记（`A.` / `A、` / `A．` / `(A)` / `（A）`，后跟空白）与前文之间补空行 → 每个选项独立段落；
  2. 行首 `"A. "` 的半角句点转义为 `"A\. "`，**防止 Pandoc fancy_lists 把选项解析成自动编号列表**（OCR 缺项/错项时自动编号会与原题字母不符；且空内容的列表项会被 Pandoc 丢弃导致字母消失——本轮 PDF 测试时实际踩到）。
- **验证**：两种真实形态（单换行连接 / 空行分隔 + 顿号）均生成"每选项一段"，字母为字面文本，行内公式仍为可编辑 OMML。

## 二十、2026-08-15 更新：多图并行识别

- **后端（api.py `process_task` 重写）**：`ThreadPoolExecutor`（`API_MAX_WORKERS=3`）并行识别——API/混合模式是网络瓶颈（单次约 20 秒），并发收益大；**本地引擎仍串行**（`local_lock`，Pix2Text 单例非线程安全，且 CPU 并行只会互相抢占）。
- **结果顺序保证**：结果按 idx 存 dict，最后按上传顺序拼接，与并行完成顺序无关；单张失败不中断（照旧进 failed 列表）。
- **线程安全**：`recognizer.py` 的版面分析单例加 `_layout_lock`（多线程混合模式会并发调 `parser.parse`，ONNX 会话需保护，仅约 2 秒无瓶颈）；`log_queue`（Queue）天然线程安全。
- **进度语义变化**：progress 事件从"开始处理第 N 张"（current=N，百分比 (N-1)/total）改为**完成计数**（current=已完成数，百分比 current/total）；`ProgressBar.jsx` 同步改为"并行识别中，已完成 N/total 张"。preview/done 兜底 100% 逻辑不变。
- **验证**：打桩测试通过——5 张图（4 混合 + 1 本地，1 张故意失败）确认真并行（API 调用时间线重叠）、顺序保持（001/002/004/005）、失败隔离、进度事件 (0..5)/5 完整。
- 注意：后端 venv 的 pythonw.exe 是指向 Kimi 运行时 Python 的链接，WMI 看到的进程命令行是解析后的真实路径，属正常现象。

## 二十一、2026-08-15 更新（二）：一页多题自动拆分

- 新增 `backend/splitter.py`：只裁剪页面**左侧边条**（页宽 18%，150~280px）做轻量 OCR 找题号，按题号行 y 坐标把整页横向切成单题子图，每张子图走现有管线成为独立"题目 N"。
- **题号识别要点**（`find_question_split_ys`）：用 `recognize_page` 的 meta 逐 token position（相对元素框，需加 box 偏移）；容忍 OCR 把题号当公式的形式（`{\bf7 .}` → 去 `\命令/{}$` 装饰后提取数字）；支持 `6.` `7、` `（8）` `第9题`；**小数保护**（`9.5` 不匹配）；题号必须**严格递增 + 相邻间距 > 3% 页高**才采信（防正文数字误检）。
- **回退安全**：不足 2 个题号或任何异常 → 整页按单题处理，静默不报错。`SPLIT_TOP_MARGIN=24`（OCR 题号框偏矮，边距小了会切到题号字形顶部，12 实测不够）。
- **接入**（api.py）：拆题在并行识别**之前的串行预处理阶段**执行（Pix2Text 非线程安全）；展开为工作项列表 `(label, 子图路径, 引擎)`，拆分后的子题继承原图引擎；日志 label 为"xxx.png 第 N 题"。`/upload` 新增 `split` 表单字段（"1" 默认 / "0" 关）。
- **前端**：输出设置新增「整页拆题」Switch（默认开），上传时附带 `split` 字段。
- **性能提示**：拆题检测需初始化 Pix2Text（纯 API 用户首次约 14 秒，之后驻留），每页边条 OCR 约 10-30 秒（含 MFD），换来的是多题全自动拆分。
- **验证**：合成两题页（复制真题改题号纵向拼接）拆成 2 张边界干净的子图；单题页原样返回；编排测试（真实 splitter + 打桩识别）通过——1 页拆 2 题、顺序正确、进度事件完整、题号剥离对每子题生效。
- **真实必刷题页面适配**（2026-08-15 晚，用户实测图：大灰美术字题号"4/5"紧跟出处标签"[2026 …质检，中]"，页面无缩进）：
  - **正文顶格数字误检**：无缩进页面换行顶格，"设该柱体高为 40 cm"的"40"曾被当题号。修复：① 题号只认段落**首行**（`line_number==0`）的 token；② 题号必须**连续递增**（`prev+1`，容忍 OCR 漏一个号到 `prev+2`），正文里的 40/750 等自然被滤掉。
  - **美术字题号被 MFD 吞掉**：大灰数字"5"被公式识别连同出处标签吞成乱码 token（`\mathbb{C} [ \; 2 0 2 6`），提取不出数字。修复：首行、靠左（x < 边条宽 40%）、含 `[`（出处标签特征）的 embedding 乱码 token 记为**乱码题号候选**，按顺序推断为上一题号 +1；与上一题号同一行（间距 ≤3% 页高）的乱码跳过（那是已识别题号同行的出处标签）。
  - **切分边距**：美术字题号字形比 OCR 文本框顶部高出约 25px，`SPLIT_TOP_MARGIN` 24→36，实测切线落在上一题选项行下沿与题号字形上沿之间的空白带。
  - 验证：真实图拆成 2 题，part_1 含完整第 4 题（选项+立方体插图），part_2 含完整第 5 题（题号"5"完整+两张照片插图）；合成两题页/单题页回归通过。

## 二十二、2026-08-15 更新（三）：识别后处理——选项拆分 / 填空还原 / 图注配对

- **选项挤一行**（converter.py `normalize_option_lines`）：本地 OCR 常把 A/B/C/D 四个选项识别成**同一行**，甚至把"字母+句点"开头的选项整体包进公式（`$\mathrm{C . ~} 1 ~ 2 0 0$`）。新增：① 公式包裹的选项标记解包（`$…C . ~ 剩余$` → `C. $剩余$`，仅字母+句点开头才解，`$F_{1}$` 等不受影响）；② 行内 `B.`/`C.`/`D.` 标记前强制分段；③ 行首规则放宽（`A.600` 句点后无空格也认）。在 `elements_to_markdown`（预览可见）与 `merge_questions`（用户编辑后兜底）各做一遍，幂等。
- **填空下划线还原**（`restore_fill_blanks`）：填空下划线是孤横线，OCR 和多模态模型都会丢。启发式：`（填`/`(填` 前没有下划线则补 `\_\_\_\_\_\_`（转义防 Pandoc 解析成加粗）。**局限**：不带"（填…）"提示的留空无法识别。API 提示词也加了"下划线必须保留为 ______"的要求，双保险。
- **图注配对**（`_pair_captions`）：recognizer/api_recognizer 的元素现在带 `box` 坐标。短标签（甲/乙/图1）配对规则：① 独立小元素按位置配对（标签水平中心落在图 x 范围、紧跟图下沿 80px 内）；② 大段文字尾部的独立图注行（API/混合模式）按阅读顺序分配；③ 有图注的插图按图注顺序排序，缺注的按"甲乙丙"序列推断补齐；配对后输出 `![甲](...)`，Word 里显示为图片下方图注。阅读顺序（`_reading_order`）：纵向重叠过半算同一行，行内按 x——修复了 OCR 把右侧乙图排在左侧甲图前的问题。API 提示词同步要求图注作为独立行放在 text 末尾。
- **验证**：单元测试 14 项全过（选项拆分/公式安全/幂等/填空/两种图注形态）；真实图端到端：第 4 题 A/B/C/D 各自独立段落，第 5 题两个空渲染为 `______`，甲乙图注正确配对且甲图在前。回归测试保留在 `backend/_test_cleanup.py`（拆题回归在 `_test_split2.py`）。

## 二十三、2026-08-15 更新（四）：历史任务 / API 用量统计 / 粘贴上传

- **历史任务列表**：每个任务的关键信息（原始文件名、引擎、最终 Markdown、用量、输出文件）落盘到 `temp/<task_id>/task.json`；启动时 `load_persisted_tasks()` 扫描恢复，重启后历史任务仍可下载。保留策略：无结果任务 2 小时清理，有识别结果的任务保留 **7 天**。新接口：`GET /history`（列表摘要）、`GET /history/{id}`（含最终 Markdown）、`DELETE /history/{id}`（删文件+记录）。「重新导出」= 取回最终 Markdown + 当前输出设置（格式/字体/标题前缀）调 `/finalize` 重新生成，**无需重新识别**；用户预览编辑后的版本会在 finalize 时覆盖存回 task.json。
- **API token 用量**：`_call_chat_completions` 返回值改为 `(content, usage)`，usage 取自 OpenAI 兼容响应的 `usage` 字段（qwen/智谱/豆包/gemini 均返回）。逐次累计到 `backend/api_usage.json`（total + by_model，加锁写入）；`GET /usage` 返回累计统计；每次识别日志显示单次用量，预览事件带任务级汇总。费用不内置价格表，前端提示按服务商刊例价自行估算。
- **粘贴上传**：App.jsx 全局 `paste` 监听，剪贴板图片直接加入上传列表（自动命名 `粘贴截图_MMDD_HHMMSS.png`，默认混合引擎）；输入框/文本域聚焦时不拦截，处理中不响应。
- **前端**：新增「历史任务」按钮 + `HistoryPanel` 弹窗（时间/题数/用量/失败数，Word/PDF 直下载、重新导出、删除）；「API 设置」弹窗底部新增累计用量区（每次打开刷新）。
- **验证**：端到端测试 `backend/_test_history.py` 全过——上传识别 → finalize → 历史列表/详情 → 改 PDF 重新导出（不重新识别）→ 下载；重启后端后历史任务从 task.json 恢复、文件仍可下载。用量统计因 qwen key 在浏览器本地，只验证了接口与累计逻辑，首次真实 API 识别后即可见数。

## 二十四、2026-08-16 更新：多题插图混排的跨题重分配

- **场景**：一页两题，但两题的插图并排放在两题文字中间（图注"（第 2 题图）""（第 3 题图）"各自标归属）。横切拆题会让插图带整体归上一题。
- **方案**：图注题号驱动的跨题插图重分配。① `split_image` 返回 `(子图路径, 题号)`；② 图注正则扩展认「（第 N 题图）」，且容忍 OCR 把数字包成小公式（`（第 $~ 3$ 题图）` → 清洗 `$~/命令/空白` 后匹配）；③ 新增元素级图注配对 `tag_figure_captions`（识别后即配，插图元素写 `caption`）；④ 新增 `reassign_question_figures`：全部识别完成后，图注题号与所在题不符的插图搬到对应题；对不上题号的原地保留（安全兜底）。
- **编排**（api.py）：`recognize_one` 改为返回元素列表（图注配对在其中完成），全部并行识别结束后先跨题重分配、再逐题生成 Markdown。
- **验证**：真实混排图端到端——第 2 题只带拉力图（图注"第2题图"），第 3 题只带滑轮组图（图注"第3题图"），裁剪图内容逐一目检无误；图注文字不再残留在正文。单元测试新增 7 项（`_test_cleanup.py`，含公式包裹图注、搬移、兜底保留），拆题回归适配新返回值后通过。
- **已知局限**：① 无"第 N 题图"标注的混排插图无法归属（空间规则兜底未做）；② 双栏选项排版（A/B 一行、C/D 一行）本地 OCR 会串行，需混合/API 模式的模型文本才好拆。
- **用户混合模式实测暴露的两个连环坑（已修）**：① 「（第 2 题图）」图注里的"第 N 题"被拆题正则当题号命中，多切出一个只含图注的假题，图注与插图被切分线隔开、配对失效——修复：题号正则加负向断言 `第 N 题(?!\s*图)`，且含"题图"的 token 一律不参与题号判定；② 混合/API 模式文字是无坐标大段，图注行藏在大段尾部，而跨题重分配发生在生成 Markdown 之前、此时图注尚未配对——修复：大段尾部图注行提取从 `_pair_captions` 上移到 `tag_figure_captions`（识别后立即执行），按阅读顺序配给未配对插图。回归测试新增混合形态用例（共 25 项全过），本地模式复测无回归。
- **第二轮实测（题1杠杆+题2滑轮混排页）又修三处**：① 题号 2 被 MFD 吞成 **isolated** 型乱码（`\mathbb{Z [}…`），乱码候选原来只认 embedding 型——补上 isolated；② 插图带被框出"整带大框+单图小框"的嵌套框，去重规则原来用"交集/候选框面积"单向比，大框永远判不重复——改成与两者中**较小面积**比（recognizer `_dedup_figure_boxes`，`detect_figure_crops` 与 `recognize_question` 两条路径都接）；③ 图注被大框包进插图里导致文字丢失/图内带字——配对后若图注框在插图框内部，把已裁图片下沿收紧到图注上方（`_crop_caption_out`）；落单无注插图在"第 N 题图"风格的页面里补默认图注 `第{本题号}题图`。
- **最终效果**：该页拆 2 题，第 1 题只带杠杆装置图（图注"第1题图"），第 2 题只带滑轮组图（图注"第2题图"，跨题搬移），裁剪图目检干净无图注字、无重复图。

## 二十五、2026-08-16 更新（二）：Word / PDF 文件直识别

- **需求**：除截图外，直接上传 Word / PDF 文件完成题目识别与重排。
- **方案**（新增 `backend/doc_loader.py`，三条路线）：
  - **PDF** → PyMuPDF 按页渲染 180 DPI 高清图（`pdf_to_images`），走现有图片识别管线。试卷 PDF 的文字层公式是字体排版、抽出来是乱码，渲染成图走 OCR 才能拿到 LaTeX。支持页码范围（`parse_page_range`："1-3,5" 解析，容错全角分隔符，非法输入 400），单次上限 20 页防整本上传。
  - **Word 直转（默认，推荐）** → pandoc `-f docx -t markdown --extract-media`（`docx_to_questions`）：文字公式零误差，OMML 公式自动转 LaTeX，内嵌图片提取到任务目录 `direct/fNN/media/`，按题号行切成一道道题，**不占识别额度、秒级完成**。
  - **Word OCR（可选）** → docx → PDF（`pdf_builder.docx_to_pdf` 的 Word/WPS/LibreOffice 回退链）→ 页图 → 图片管线，用于"Word 里嵌的是题目截图"。
- **直转切题踩坑**：① 题号正则漏了 `4[2026江苏…]` 这种"题号紧跟年份来源标注"的无点号形式（pandoc 转义为 `4\[2026`），补上 `\d{1,3}\s*\\?\[(?:19|20)\d{2}`；② pandoc 按 `--extract-media` 入参原样写图片引用，传绝对路径就得到带反斜杠的绝对路径（前端预览和合成 docx 都用不了），统一改写成相对任务目录的路径；③ 本工具导出的 Word 再导入时 `## 题目 N` 标题行识别为导出产物丢弃。多个 Word 同任务时媒体按 `direct/fNN/` 分目录防 rId 重名覆盖。
- **编排**（api.py）：上传接口新增 `pages` / `docmodes` 两个 JSON 数组表单字段，上传时即展开成有序 `plan`（图片项 / preset 直转题项），展开耗时操作（docx→PDF 走 COM 可能几十秒）放 `run_in_threadpool`；`process_task` 改为消费 plan——preset 项在 `recognize_one` 里短路返回（套与识别结果相同的选项分行/填空还原后处理，预览风格一致），真实识别项照旧跨题插图重分配（只对 elements 项做，顺序映射不错位）；老任务无 plan 时从 files/engines 重建纯图片清单兼容。
- **前端**：上传区放开 pdf/docx（图标占位卡片）；PDF 卡片带页码范围输入框（placeholder "全部，如 1-5,8"）；Word 卡片带模式切换（直转重排推荐 / OCR 识别），OCR 模式才显示引擎选择；直转模式 engine 固定 local 避免误触发 API 配置校验。
- **验证**：`_test_docloader.py` 15 项单元测试全过（页码解析 9 项 / PDF 渲染 DPI / 直转切题）；端到端四组——① Word 直转全链路（2 题切分、图片相对路径、`/task-files` 200、finalize 生成 docx 且 3 张图嵌入、选项各自成行）；② PDF 指定第 1 页本地识别成功；③ 非法页码 `abc` 返回 400 中文提示；④ 图片+Word 混合上传，结果顺序与上传一致（识别题在前、直转题在后）。
- **注意**：docx 直转的题目保留原文档题号（如"4."），不剥号——这是真实文档编号，与识别题的剥号策略不同，属有意为之。
- **真文件实测（2026-08-16 下午）**：① 双 Word 同任务直转（多题测试_演示.docx 2 题 + physics_kahti.docx 1 题 19 个公式）：3 题切分正确、媒体按 `direct/f01`/`f02` 隔离无重名覆盖、合成 docx 4 图全嵌入、28 个 OMML 公式全部还原为 Word 原生可编辑公式；② 真实 PDF（必刷题示例，2 页）本地识别：第 1 页题目识别正常，但第 2 页是换页滑出去的 v-t 图（整页只有一张图），被单列成一道只有图的"题"。
- **实测暴露问题已修：纯图项并入上一题**（`converter.merge_figure_only_items`）：整项只有插图没有文字时并入前一个识别项（PDF 换页 / 拆题切出的纯图页不再单列成题）；首项纯图无前项可并时保留原样；preset 直转项不参与合并。合并在跨题插图重分配之前做，带"第 N 题图"注的图合并后仍会搬对题。回归测试新增 3 项（共 28 项全过），真实 PDF 复测：2 页合并为 1 题 4 图，finalize 生成 docx 正常。
- **用户实测杠杆题（四宫格插图 + 表格）暴露三问题，均已修**：
  1. **丢图（核心）**：版面分析输出"整带大框 + 甲丙两幅小框"，去重丢大框导致只被大框覆盖的乙丁两图彻底丢失。修复：`recognizer.recover_dropped_figures`——对被丢大框做矩形减法求未覆盖区域（`_rect_subtract`），再按网格线切分（`_split_by_grid_edges`，已保留小图框的边缘即网格线；区域偏高用水平切线、偏宽用垂直切线；切出的薄条多为图注带，并入相邻条），乙丁完整回收为两幅独立裁图。混合（`detect_figure_crops`）与本地（`recognize_question`）两条路径都接入。
  2. **图注丢失**：混合模式文字是无坐标大段，图注行靠尾部提取配对，但守卫是全或无（图注数 ≤ 空闲插图数才配）——4 图注 vs 3 插图直接放弃，甲乙丙丁残留正文末尾。根因是丢图，图修好后 4=4 自动配对成功，无需改守卫。
  3. **表格一表两出**：API 已输出 Markdown 表格，本地又把表格裁成图片。修复：混合模式检测 API 结果含 Markdown 表格（`---`+`|` 行）时跳过表格裁图（`skip_tables`）；裁图元素新增 `fig_kind` 标记，表格一律不参与图注配对与缺注推断（防表格被配"戊"）。
  - 验证：回归测试新增 7 项（含真实检测框坐标的回收/切分用例，共 35 项全过）；原图复测裁出甲乙丙丁 4 幅（乙图带鸭子装饰贴纸属原图内容），模拟混合装配图注 甲→甲、乙→乙、丙→丙、丁→丁 全部配准无残留。
  - 备注：曾尝试行投影（墨水密度）切分剩余区域，实测乙图弹簧秤斜线墨水稀疏会被腰斩，弃用，改用确定性的网格线切分。

## 二十六、已知边界与后续方向

- 纯 CPU：单张约 20-60 秒，触发 UniMERNet 二次识别更慢
- UniMERNet-small 偶有 `\iota/l` 混淆；若需更高质量可换 UniMER-Base（4.9GB，重）
- 更大升级空间：MinerU（试卷类文档解析第一梯队，需评估硬件）
- 清理规则可继续按题型扩充（化学方程式、积分/根号等）
- 用户机器上曾出现后端进程静默死亡（疑似安全软件或窗口误点冻结），已加 `backend/backend.log` 黑匣子日志
- **功能候选（已与用户对齐优先级）**：① 识别结果预览编辑（✅）② 一页多题自动拆分（✅ 左侧边条 OCR 找题号切页）③ 多图并行识别（✅ API/混合并发 3 张）④ 表格识别为真表格 ⑤ 历史任务列表/重复下载（✅ 7 天保留 + 改格式再导出）⑥ 粘贴截图上传（✅ Ctrl+V）⑦ API token 用量显示（✅ api_usage.json 累计 + 设置弹窗展示）⑧ 开机自启 ⑨ 题库模式（SQLite 积累检索）⑩ 导出 PDF（✅）/ Anki ⑪ 数字粘合清理（公式里拆开的多位数字合并）⑫ 答案/解析识别与题目对齐 ⑬ Word / PDF 文件直识别（✅ PDF 页渲染 + 页码范围；Word pandoc 直转切题 / OCR 双模式）
- API 模式边界：无逐公式精确坐标，校验失败的公式只能保留原文（红底提示已加在预览里）；qwen-vl-max bbox 定位不可靠，插图裁剪走混合模式的本地版面分析
